@model PieListViewModel

<h1>@Model.CurrentCategory</h1>

@foreach (var pie in Model.Pies)
{
    <partial name="_PieCard" model="pie" />
}
    @*<table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Price</th>
                <th>Category</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var pie in Model.Pies)
            {
                <tr>
                    <td>@pie.Id</td>
                    <td>@pie.Name</td>
                    <td>@pie.Price</td>
                    <td>@pie.Category.Name</td>
                </tr>
            }
        </tbody>
    </table>*@
